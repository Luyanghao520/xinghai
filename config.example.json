{
  "_comment": "复制本文件为 config.json 并填入强密钥，或直接设置环境变量 XINGHAI_ADMIN_KEY / XINGHAI_SECRET（优先级：环境变量 > config.json > 内置默认）。config.json 已被 .gitignore 排除，绝不提交到仓库。",
  "ADMIN_KEY": "REPLACE_WITH_STRONG_ADMIN_KEY",
  "SECRET": "REPLACE_WITH_STRONG_SECRET"
}
