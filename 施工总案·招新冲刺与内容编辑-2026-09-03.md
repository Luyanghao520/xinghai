# 施工总案 · 招新冲刺 + 官网内容编辑（2026-09-03 定稿）

> **文档性质**：唯一施工依据。由主模型对三份输入文档（需求规格 / 问题清单 / 速览 HTML）做可行性评估、事实核查、合并内化后产出。
> **执行者**：免费低端执行模型（机械照做）+ owner（人工决策 / 部署 / 验收）。
> **代码基线**：git `de0d766`（本地 main，**领先 origin 1 commit，owner 尚未推送——见批次C 第 0 项**）。
> **硬时限**：2026-09-06（周六）8:00 招新。批次 A/B 必须在 9/5 晚前完成并部署。

---

## 0. 给执行模型的协议（每次开工前先读这节）

### 0.1 硬规则（违反任何一条 = 立即停手报告）

1. **一次只做一个工作项**。做完 → 跑完该项验收 → `git commit` → 才允许开下一项。
2. **失败即停**。任何一步与本文档描述不符（行号对不上、函数不存在、验收不过），停止该项，在 `施工进度.md` 登记「工作项编号 + 卡在哪 + 报错原文」，**不得即兴改其他文件绕过**。
3. **禁止 push**。所有 commit 留在本地，owner 验收后自己推。
4. **禁止 build**，唯一例外：批次 B（第 4 节）明确授权的那一次。
5. **禁区清单**（除非该工作项明确写了要改，否则一行都不许碰）：
   - `portfolio-landing/src/index.css` 的背景三段（`bg-kenburns` keyframe / `.bg-video*` / `.bg-nebula*`）
   - `static/css/bg.css`、`static/js/bg.js`
   - `portfolio-landing/src/components/FixedBackground.tsx` —— **唯一例外**：批次 B 的 B1/B2 允许且只允许改动其看门狗块与 preload 属性
   - `content.json` 的字段结构（允许改值，禁止增删字段）
   - `wsgi.py`、`Procfile`、`requirements.txt`（本总案没让你动就不动）
   - `registrations.db` 等所有 `.db` 文件（只读，除非工作项明确说改 schema）
6. **行号为参考**。以 grep 到的实际代码为准；diff 出入超过 ±30 行说明基线漂了，停手报告。
7. 每完成一个工作项，在 `施工进度.md` 表格勾一行（编号 / commit hash / 验收结果 / 时间）。

### 0.2 环境与命令（Windows 本机，owner 机器）

```powershell
cd "C:\Users\陆阳昊\Desktop\星海艺术团官网建设"
$env:PYTHONPATH = 'pylibs'
py -3.12 app.py          # 本地服务 http://127.0.0.1:8000（改动 app.py 后重启此进程才生效）
```

- 依赖在工作区 `pylibs\`（Flask 3.1.3 等 10 个包）。**不要 pip install 任何新包**（除非该工作项明确要求，本总案没有任何工作项要求新装包）。
- 浏览器验收统一用 `http://127.0.0.1:8000/...`，禁止 file:// 打开。

### 0.3 提交规范

```
<type>(<scope>): <一句话中文>

- 要点1
- 要点2
验收：<跑了哪些验收项，结果>
```

`type` ∈ `feat|fix|perf|security|docs|chore`；`scope` ∈ `app|admin|cms|site|landing|recruit|work|deps`。
示例：`security(admin): /admin 改会话鉴权，移除 URL 明文 key`。

### 0.4 验收的三种手段（每个工作项标注了用哪种）

- **curl 检查**：`curl -s -I http://127.0.0.1:8000/<路径>` 看响应头 / 状态码
- **浏览器走查**：owner 或执行模型用浏览器按步骤点（涉及视觉的必须 owner 过目）
- **脚本压测**：仅 A5 有，本文档给出现成脚本

---

## 1. 三份输入文档的评估结论

### 1.1 《需求规格·官网内容编辑与二次加密》—— **采纳率 95%，评级：优秀**

| 维度 | 评价 |
|---|---|
| 可行性 | **高**。一期（纯后端 + 新页面）不碰 React 构建产物，招新前可上；二期推到招新后的时机判断正确 |
| 优点 | ① 自包含、带行号证据，是真·spec-driven；② 安全设计专业（独立 SITE_EDIT_KEY / POST body 传参 / `hmac.compare_digest` / DB 落地的防爆破计数 / 原子写 / 备份 / 审计 / 回滚）；③ 分层鉴权（登录→角色→二次口令）与现有装饰器体系无缝衔接 |
| 缺陷（本总案已修） | ① **`/api/site/content` POST 接受任意顶层键**——恶意/失误请求可注入未知字段污染 content.json，必须加字段白名单 + 类型校验（见 A6 第 3 步）；② `site.html` 的 `render()` 留空且字段清单与 content.json 实际结构存在出入（dept_desc 条数两处描述不一致）——改为**运行时从 JSON 派生表单**，彻底消灭清单漂移类 bug（见 A6 第 5 步）；③ 上传槽位只查扩展名——与 P1-7 合并后统一用内容嗅探（见 A4） |
| 遗留正确 | §13 登记的 4 条遗留问题全部认可，纳入批次 D |

### 1.2 《官网问题清单与解决方案》—— **采纳率 88%，评级：良好（含 3 处事实错误已修正）**

| 维度 | 评价 |
|---|---|
| 可行性 | P0 七条全部可落地；P1 各条方案 A 均为业界标准做法 |
| 优点 | ① 每条带实测证据 + 验收标准，可直接转验收单；② P0-1/2/3「传播-性能-稳定性三件套」的协同分析正确；③ P0-7 指出 WAL 模式下 tar 备份前必须 checkpoint，这个细节非常关键 |
| **事实错误（执行模型必须按本文档的修正版做）** | ① **P1-7 声称「Pillow 已在依赖里」= 错**。`requirements.txt` 只有 Flask + python-docx。修正：改用**零依赖 magic-bytes 内容嗅探**（A4），不加新依赖（PA 免费版磁盘 / 装包都金贵）；② **P0-1 首页 og 标签需要重新 build**（`landing/index.html` 是构建产物，手改会在下次 build 被覆盖）——修正：首页 og 标签挪到批次 B，与 P0-3 合并成**唯一一次 build**；③ P0-5 工作量低估——`admin.html` 的 JS 逻辑（L61/67/68/76/101）全部依赖 `?key=`，「1 小时」实际要连页面 JS、rows 接口、delete 接口、导出链接一起切到会话态（A3 已把范围写全） |
| 排期修正 | P1-2（pbkdf2 密码迁移）虽标 P1，但动登录链路，**招新前 3 天禁止做**——挪入批次 D 第一项。P1-1（随机初始密码）只影响全新部署、不碰现有库，保留在批次 A |

### 1.3 《官网问题清单速览.html》—— 派生物

前者的渲染版，无独立信息量。**建议**：随批次推进会过时，招新后归档或删除，避免双源不一致。

### 1.4 从高星项目汲取的工程实践（本总案的编排依据）

- **Spec-driven Development**：两份输入文档本身就是好实践（需求先于代码、验收先于实现）。本总案延续：每个工作项 = 目标 → 步骤 → 验收 → 回滚 → 提交信息，低端模型不需要「理解」，只需要「执行 + 核对」。（参考：Towards Data Science《From Vibe Coding to Spec-Driven Development》、Addy Osmani《How to Write a Good Spec》、Red Hat《Vibes, Specs, Skills, and Agents》2026-03）
- **AGENTS.md 仓库约定**（本地参考库 `.tools/ref-ui/daisyui/packages/AGENTS.md` 的范式）：工作区规则 / 命令表 / 沟通纪律，全部收敛到本文档 §0——**低端模型不读项目里其他任何文档也能开工**，避免上下文超载。
- **单次构建批处理**：所有需要 `npm run build` 的改动合并到批次 B 一次做掉，把构建风险窗口从 N 次压缩到 1 次（招新前最忌反复动产物）。
- **事实核查先于编码**：Pillow 事件证明——低端模型会照抄文档里的错误前提。本总案所有行号 / 依赖 / 字段清单已在定稿前逐项 grep 核实。
- **不可逆操作隔离**：push / build / 部署 / 数据库 schema 变更全部留给 owner 或批次 D，低端模型的操作面限定为「可 git checkout 单文件回滚」的范畴。

---

## 2. 总排期（四批次）

| 批次 | 时限 | 内容 | 需要 build | 谁 |
|---|---|---|---|---|
| **A** | 9/3 晚 – 9/4 | 十个后端 / 静态页工作项（性能加固 / 安全清理 / 站点编辑一期） | ❌ | 执行模型 |
| **B** | 9/4 – 9/5 | 首页三件套（看门狗限次 / og 标签 / 可选弱网降级）+ **唯一一次 build** + 部署 | ✅ 唯一一次 | 执行模型改码，owner 验收部署 |
| **C** | 9/5 – 9/6 | 招新日操作单（演练 / 群码 / 备份）——全部人工 | — | owner + 各分团长 |
| **D** | 招新后 1–4 周 | 密码迁移 / CSRF / 站点编辑二期 / OSS / schema | 视项而定 | 下一轮会话 |

**批次内顺序即优先级**：A1→A2→A5→A3→A4→A9→A7→A10→A6（站点编辑一期最大，放最后，做不完不阻塞招新）→A8。

---

## 3. 批次 A · 工作项明细（执行模型照做）

> 每项格式：**目标 / 改哪里 / 怎么做 / 验收 / 回滚 / 提交信息**。涉及 app.py 的项，行号以 grep 实际为准。

### A1 缓存头 + 安全响应头 + 413 错误处理（P0-2 方案 A + P1-4 + 413）

**目标**：静态资源带缓存头（视频/图片不再每次全量重下），四个安全响应头就位，超限上传返回友好 JSON。

**改哪里**：`app.py`，在 `app = Flask(...)` 与 `app.config.update(...)` 之后的任意位置追加：

```python
@app.after_request
def xh_headers(resp):
    p = request.path
    if p.startswith("/assets/"):
        resp.headers["Cache-Control"] = "public, max-age=31536000, immutable"
    elif p.startswith("/static/") or p.startswith("/showcase/") or p.startswith("/u2026"):
        resp.headers["Cache-Control"] = "public, max-age=604800"
    else:
        resp.headers["Cache-Control"] = "no-cache"
    resp.headers.setdefault("X-Content-Type-Options", "nosniff")
    resp.headers.setdefault("X-Frame-Options", "SAMEORIGIN")
    resp.headers.setdefault("Referrer-Policy", "strict-origin-when-cross-origin")
    resp.headers.setdefault("Permissions-Policy", "geolocation=(), microphone=(), camera()")
    return resp

@app.errorhandler(413)
def e413(e):
    return jsonify({"ok": False, "error": "上传内容超过 10MB 上限"}), 413
```

注意：HSTS 与 CSP **本批次不加**（CSP 会白屏整站的内联脚本；HSTS 等 HTTPS 观察一周后再说——见 D 批次）。

**验收**：
1. 重启本地服务后：`curl -s -I http://127.0.0.1:8000/showcase/sc02.jpg` 含 `Cache-Control: public, max-age=604800`
2. `curl -s -I http://127.0.0.1:8000/` 含 `X-Content-Type-Options: nosniff` 与 `X-Frame-Options: SAMEORIGIN`
3. 浏览器打开首页 + 招新页 + `/work`（登录态）各一次，功能正常（no-cache 只作用于 HTML，无副作用）

**回滚**：`git checkout app.py`。**提交**：`perf(app): 静态资源分级缓存头 + 安全响应头四件套 + 413 JSON`

### A2 SQLite 并发加固 + CSV 节流（P0-6 方案 A）

**目标**：招新高峰并发写不炸；报名不再每次触发全量 CSV 重写。

**改哪里**：
1. `app.py` 的 `db()` 函数（约 L65），替换为：

```python
def db(path):
    cx = sqlite3.connect(path, timeout=15)
    cx.row_factory = sqlite3.Row
    cx.execute("PRAGMA journal_mode=WAL")
    cx.execute("PRAGMA busy_timeout=15000")
    cx.execute("PRAGMA synchronous=NORMAL")
    return cx
```

2. `export_csv()`（grep 定位）函数体开头加节流；同时 grep 全部调用点：报名成功 / 删除记录的调用保持节流（不传 force），`/admin` 的 `export=csv` 分支与任何「导出给用户看」的调用点改为 `export_csv(force=True)`。

```python
_LAST_CSV = 0.0
def export_csv(force=False):
    global _LAST_CSV
    import time as _t
    _n = _t.time()
    if not force and _n - _LAST_CSV < 30:
        return
    _LAST_CSV = _n
    ...  # 原逻辑不动
```

**验收（必须真压测，不许只看代码）**：

```bash
# 另开一个终端，服务跑起来后：
py -3.12 -c "
import concurrent.futures, urllib.request, urllib.parse, json
def hit(i):
    d = urllib.parse.urlencode({'target':'测试团','name':'压测%d'%i,'gender':'女','birth':'2008-01-01','campus':'浦东','college':'测试学院','major':'测试','phone':'138%08d'%i,'wechat':'w%d'%i,'reason':'x'}).encode()
    r = urllib.request.urlopen('http://127.0.0.1:8000/api/signup', d, timeout=30)
    return json.loads(r.read()).get('ok')
with concurrent.futures.ThreadPoolExecutor(20) as ex:
    rs = list(ex.map(hit, range(50)))
print('ok=', sum(1 for x in rs if x), '/50')
"
```

期望：`ok= 50 /50`（若字段名与实际表单不符，先 grep `/api/signup` 的取参名对齐再压）。再 `sqlite3 registrations.db "SELECT COUNT(*) FROM registrations;"` 确认行数=50+压测前值，然后**把这 50 条压测数据删掉**（`DELETE FROM registrations WHERE name LIKE '压测%';`）。

**回滚**：`git checkout app.py`；若 WAL 已开启想回退：`sqlite3 <各库> "PRAGMA journal_mode=DELETE;"`。**提交**：`perf(app): SQLite WAL+busy_timeout，报名并发 50 连发实测通过；CSV 导出 30s 节流`

### A3 /admin 全链路改会话鉴权（P0-5 + P1-6 后台半边）

**目标**：URL 里不再出现任何 admin 密钥；招新当天主席从工作台点进去就能用。

**改哪里**（四点，缺一不可）：
1. `app.py` 的 `/admin` 路由（grep `@app.route("/admin")`）：函数装饰器改为
```python
@app.route("/admin")
@login_required
@chair_required
def admin():
```
（`?export=csv` 分支逻辑不动，现在它天然被上面两层保护。）装饰器顺序：`@app.route` 最上，`@login_required` 其次，`@chair_required` 最后。
2. `/api/admin/rows`（grep 定位）：删除 `request.args.get("key")` 校验分支，装饰器同样加 `@login_required` + `@chair_required`。
3. `/api/delete`（grep 定位）：删除 body 里 `key` 字段校验，装饰器同上。
4. `admin.html`：删除 L61 `const KEY=params.get('key')||'';` 与 L67-68 的空 KEY 拦截块；L76 rows 请求改为 `fetch('/api/admin/rows?q='+encodeURIComponent(q))`，并把 403 分支改为 `location.href='/login'`；L101 delete 的 body 去掉 `key:KEY`；L68 导出按钮改为 `$('exportBtn').href='/admin?export=csv';`。

**全库复查**：`grep -n "admin?key\|&export=csv" *.html .tools/*.html 2>/dev/null`——`work.html` L91（批次 A 的 A3 顺带改：`<a class="xh-mod" href="/admin" target="_blank">`）、`.tools/dbg-stage.html` 里的带 key 链接全部去参。`.tools/` 下调试页只改链接，不做其他动作。

**验收**：
1. 未登录 `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/admin` → 302（跳登录）
2. 成员角色登录访问 `/admin` → 403；主席/副主席登录 → 正常显示列表，导出 CSV 下载成功
3. `grep -rn "key=" admin.html work.html | grep -v "keyInput"` → 0 命中

**注意**：`/api/upload`、`/api/content` POST、`/api/kb` 写操作仍走 key（cms 在用），**本项不许碰**——它们在批次 D 随 cms 迁移一起处理。

**回滚**：`git checkout app.py admin.html work.html`。**提交**：`security(admin): /admin/rows/delete 全链路改会话鉴权，清除全部 URL 明文 key`

### A4 上传内容嗅探（P1-7 修正版：零新依赖）

**目标**：`.txt` 改名 `.jpg` 上传被拒；不加任何新包。

**改哪里**：`app.py`，在 `chair_required` 装饰器后加共享工具：

```python
_IMG_MAGIC = (
    ("jpeg", b"\xff\xd8\xff"),
    ("png",  b"\x89PNG\r\n\x1a\n"),
    ("gif",  b"GIF87a"),
    ("gif",  b"GIF89a"),
)
def sniff_image(raw):
    for fmt, m in _IMG_MAGIC:
        if raw.startswith(m):
            return fmt
    if raw[:4] == b"RIFF" and raw[8:12] == b"WEBP":
        return "webp"
    return None
```

然后改造 `/api/upload`（grep 定位）：在读入文件后、保存前插入：

```python
raw = f.read()
if len(raw) > 5 * 1024 * 1024:
    return jsonify({"ok": False, "error": "图片不能超过 5MB"}), 400
if not sniff_image(raw):
    return jsonify({"ok": False, "error": "文件内容不是有效图片"}), 400
import io as _io
f.stream = _io.BytesIO(raw)
```

（放在扩展名白名单校验之后、`f.save(...)` 之前；`f.stream` 重置后原 save 逻辑不用动。）

**验收**：把一个 txt 改名 `test.jpg`，用 cms 上传或 curl `-F` 上传 → 返回「文件内容不是有效图片」；真 jpg/png 上传成功且可访问。

**回滚**：`git checkout app.py`。**提交**：`security(app): 上传加 magic-bytes 内容校验（零依赖），拒绝伪装图片`

### A5 404 / 500 错误页（P1-8）

**目标**：深色风格 404/500 页替代 Flask 默认白页。

**改哪里**：
1. 新建 `404.html` / `500.html`（仓库根，与现有系统页同构）：引 `/static/css/theme.css` + `ui.css` + `bg.css`，顶栏用 `.xh-topbar`（复用 work.html 的顶栏结构，登出按钮可省），正文一个 `.xh-panel`：大字「404 页面走丢了」/「500 服务器开小差了」+ 两个按钮「返回官网 `/`」「去报名 `/recruit`」。500 页文案附一句「请截图时间点联系技术负责人」。
2. `app.py` 追加：

```python
@app.errorhandler(404)
def e404(e):
    if request.path.startswith("/api/"):
        return jsonify({"ok": False, "error": "接口不存在"}), 404
    try:
        return send_file(os.path.join(BASE, "404.html")), 404
    except Exception:
        return "404", 404

@app.errorhandler(500)
def e500(e):
    app.logger.exception(e)
    if request.path.startswith("/api/"):
        return jsonify({"ok": False, "error": "服务器内部错误"}), 500
    try:
        return send_file(os.path.join(BASE, "500.html")), 500
    except Exception:
        return "500", 500
```

**验收**：`curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/no-such-page` → 404 且返回 HTML；`/api/no-such` → 404 JSON；浏览器看两页样式与整站一致。**回滚**：`git checkout app.py && rm 404.html 500.html`。**提交**：`feat(app): 深色风格 404/500 错误页 + API JSON 分支`

### A6 站点编辑一期（需求规格 §5+§6 全量 + 本总案三处修正）

> 最大单项，放批次 A 最后。做不完可整体顺延到招新后，**不阻塞招新**（前置项 A1-A5 全部完成后再开工本项）。

**前置**：owner 在 `config.json` 增加 `"SITE_EDIT_KEY": "<32位随机串>"`（owner 自备），并确认 `.gitignore` 已含 `config.json`。

**步骤**（严格按需求规格 §5.1→5.4→6.1→6.2 顺序执行，以下只写**与原规格的三处差异**，其余原样照抄规格书）：

1. **§5.1 配置块**：原样。追加一行 `os.makedirs(SITE_BACKUP_DIR, exist_ok=True)`（规格已含）。`init_site()` 调用追加到现有 `init_reg(); init_mem(); ...` 调用序列同行。
2. **§5.4 `api_site_content_set`（修正①字段白名单）**：在 `for k, v in data.items():` 循环前插入：

```python
ALLOW = {"hero_welcome","b0","b1","b2","b3","t_wei","t_dai","footer",
         "consult_qr","team_qr","dept_desc","carousel","org"}
TYPE = {"team_qr": dict, "dept_desc": dict, "carousel": list, "org": dict}
for k in list(data.keys()):
    if k.startswith("_"):
        data.pop(k); continue
    if k not in ALLOW:
        return jsonify({"ok": False, "error": "未知字段: " + k}), 400
    if k in TYPE and not isinstance(data[k], TYPE[k]):
        return jsonify({"ok": False, "error": "字段类型错误: " + k}), 400
```

3. **§5.4 `api_site_upload`（修正②内容嗅探）**：`f.save(dst)` 与 `f.save(...safe)` 两个分支前都加 A4 的 `sniff_image` 校验（读 raw → 校验 → 重置 stream）。槽位分支另加：上传的若是 png/gif/webp 存成 `slot+".jpg"` 会有扩展名与内容不符——**允许**（浏览器按内容渲染），但在返回 JSON 里附 `"note": "槽位文件为固定文件名，已按原内容写入"`。
4. **§6.2 `site.html`（修正③表单派生）**：`render()` 不硬编码字段清单，按下面的**分区配置表**实现（执行模型逐字照抄这张表）：

```js
// 固定文本区（这些是确定存在的顶层字段）
const TEXT_FIELDS = [
  ["hero_welcome","首页主视觉欢迎语（支持 <br> <b>）"],
  ["b0","加入理由 · 一"],["b1","加入理由 · 二"],["b2","加入理由 · 三"],["b3","加入理由 · 四"],
  ["t_wei","指导老师 · 魏启旦"],["t_dai","指导老师 · 戴旭彤"],
  ["footer","页脚文案"],
];
// 派生区（运行时遍历 JSON 的键，永不漂移）
// dept_desc → 每个键一个 textarea，label=键名
// team_qr  → 每个键一个图片上传+预览（空对象则显示提示「暂无分团群码，点击新增」+ 新增键名输入框）
// carousel → 数组渲染，每项 img(上传)+desc(textarea)，附上移/下移/删除按钮，底部「新增一项」
// org      → 拆分表单：chairman/vice_pudong/vice_songjiang 各渲染 name/desc/ico 三个输入框（ico 提示「 Lucide 图标名或 emoji」）；perform_teams/admin_teams 数组逐项渲染 name/desc/ico + 上移/下移/删除/新增
```

`collectForm()` 与 `render()` 一一对应（同样的配置表反向读取）。`org` 提交时整体作为一个对象。**org 的 ico 字段不做内容校验**（现状就是 emoji/Lucide 混用，前端只透传）。

5. **§6.1 work.html 改造**：原样（合并双卡片 / 角色显隐 / `/admin` 去 key——A3 已做去 key，此项只加两个新卡片与显隐 JS）。
6. **§6.3 安全清理**：S1（cms.html L52 去 `value="xinghai2026"`，改 `type="password" autocomplete="off"`）原样做；S3（cms.html 的 key 改 sessionStorage 暂不做——cms 输入框已清默认值，用户手动输 key 即可，**把 S3 挪入批次 D**，避免 cms 链路招新前出回归）；S4 登记遗留（已在 D 批次）。

**验收**：需求规格 §8.1 的 A1–A12 全表 + §8.2 的 B1–B5 全表（执行模型能自测的用 curl；涉及视觉与「扫码」的标注「待 owner」）。**回滚**：本项全部是新增 + 少量 work.html/cms.html 改动：`git checkout app.py work.html cms.html && rm site.html && rm -rf site.db data/`。**提交**：拆成三个：`feat(site): 二次口令解锁链路+防爆破+审计`、`feat(site): 内容编辑器与备份回滚`、`chore(site): 工作台入口与 cms 默认口令清理`

### A7 初始管理员密码随机化（P1-1 方案 A）

**目标**：全新部署不再产生已知密码的后门账号（现有线上库不受影响）。

**改哪里**：`app.py` 中 `init_usr()` 里 `SELECT COUNT(*) ... == 0` 的播种分支，`hx("xinghai2026")` 改为：

```python
import secrets as _secrets
_tmp = _secrets.token_urlsafe(12)
# ... INSERT 使用 hx(_tmp) ...
print("[INIT] 已创建初始管理员 000000000，临时密码：%s  请立即登录修改" % _tmp)
```

**验收**：`grep -n "xinghai2026" app.py` → 仅剩注释（若有）；临时重命名 `users.db` 后启动 → 控制台打印随机密码，用旧密码登录失败；**测完把 users.db 还原**。**回滚**：`git checkout app.py`。**提交**：`security(app): 初始管理员密码改为随机生成打印，消除硬编码后门`

### A8 .gitignore 与目录收尾

**改哪里**：`.gitignore` 追加两行：`site.db`、`data/`。**验收**：`git status` 不再显示未来会出现的 site.db/data。**提交**：`chore: gitignore 排除 site.db 与 data/`

### A9 recruit.html 补 Open Graph（P0-1 招新页半边）

**改哪里**：`recruit.html` `<head>` 内 `<title>` 之后插入（原样照抄）：

```html
<meta property="og:type" content="website">
<meta property="og:site_name" content="星海艺术团 · 上海立信会计金融学院">
<meta property="og:title" content="星海艺术团 2026 招新｜9月6日 8:00–17:00 两校区同步">
<meta property="og:description" content="零基础可报 · 11 个演出与职能方向 · 填完表直接进群">
<meta property="og:image" content="https://luyanghao.pythonanywhere.com/static/uploads/showcase/sc02.jpg">
<meta property="og:url" content="https://luyanghao.pythonanywhere.com/recruit">
<meta name="twitter:card" content="summary_large_image">
```

> og:image 先用现成 `sc02.jpg` 兜底（批次 C 里 owner 换裁剪版 `og-recruit.jpg` 后，把 URL 里的 `showcase/sc02.jpg` 替换为 `og-recruit.jpg` 即可——一行改动，无需 build）。

**验收**：`curl -s http://127.0.0.1:8000/recruit | grep "og:"` → 7 行。**回滚**：`git checkout recruit.html`。**提交**：`feat(recruit): 微信分享 OG 卡片标签`

### A8 与 A10 说明

原计划的「A8 pbkdf2 密码迁移」已移入批次 D（理由见 §1.2 排期修正）；本节编号保持连续，缺失即挪动痕迹。

---

## 4. 批次 B · 唯一一次 build（9/4–9/5，执行模型改码 + owner 部署）

### B1 看门狗限次退避（P0-3 方案 A）—— 禁区文件唯一例外

**改哪里**：`portfolio-landing/src/components/FixedBackground.tsx` L88–98 的 `else if (performance.now() - lastAdvance > 2200) { ... }` 块，整体替换为：

```js
} else if (performance.now() - lastAdvance > 2200) {
  if (reloads < 2) {
    void cur.play().catch(() => {});
  } else if (reloads < 4) {
    cur.load();
    void cur.play().catch(() => {});
  }
  reloads++;
  lastAdvance = performance.now() + reloads * 4000;
}
```

除该块与 B2 的 preload 属性、B3（若 owner 批准）外，**该文件其他任何行不许动**。L86–87 的正常推进清零逻辑保持原样。

### B2 video preload 调整

**改哪里**：同文件 stage 分支注入模板字符串：`bg-b`（垫底那条）的 `preload="auto"` 改 `preload="metadata"`（`bg-a` 与 star 的保持 `auto` 不动——垫底条提前全量缓冲是交叉淡化发虚的根因之一，metadata 后首切时由 play() 触发加载，配合 A1 缓存头无卡顿）。

### B3 弱网/移动端降级（P0-3 方案 B，owner 拍板项）

> **owner 睡醒后拍板：做 or 不做。** 默认**做**。执行模型若开工时此处标注未决，跳过本项先 build，owner 回来再补。

**改哪里**：`FixedBackground.tsx` 的 `useEffect` 开头（`const host = hostRef.current;` 之后）加：

```js
const conn = navigator.connection;
const slow = conn && /(^|-)2g$|slow-2g|^3g$/.test(conn.effectiveType || "");
if ((slow || conn?.saveData) && !new URLSearchParams(location.search).has("bg")) {
  setReady(true);            // 直接亮海报层，不注入 video
  return;
}
```

（`?bg=` 调试参数可强制绕过降级，方便验收对照。）

### B4 首页 OG 标签（P0-1 首页半边）

**改哪里**：`portfolio-landing/index.html`（**源码入口**，不是 `landing/` 产物）`<head>` 内 `<title>` 后插入与 A9 相同的 7 行（og:url 用 `https://luyanghao.pythonanywhere.com/`，og:title 用「星海艺术团｜以艺术，赴山海」）。

### B5 构建 + 部署（owner 亲自执行）

```powershell
cd portfolio-landing
npm run build
# 产物同步（本仓库约定）：
cp dist/index.html landing/index.html
cp dist/assets/* landing/assets/
# 清孤儿：landing/assets 里不被新 index.html 引用的旧 hash 文件删除
```

然后 owner：`git add -A && git commit`（批次 A+B 全部）→ `git push origin main`（**连同此前积压的 de0d766 一起推**）→ PA Bash `cd ~/mysite && git pull && git status` 确认 HEAD → Web 标签 **Reload**。

**验收**：
1. Chrome DevTools Network 限速 "Slow 3G" 开首页：视频请求**不出现反复重试**（修复前每 2.2s 一条）
2. 断网重连：视频最多重载 4 次后停在海报层
3. 手机访问首页：无视频流量（B3 做了的话）；海报正常
4. 分享 `https://luyanghao.pythonanywhere.com/` 到微信群 → 显示标题+描述+大图卡片（若微信死缓存，链接后加 `?v=2`）
5. 首页视觉与 build 前**逐屏一致**（除背景行为变化）——owner 目测

---

## 5. 批次 C · 招新日操作单（9/5 演练 + 9/6 当天，全部人工）

| # | 时机 | 动作 |
|---|---|---|
| C0 | **现在（owner 睡醒第一件事）** | 本机终端 `git push origin main`（积压 de0d766 + 三份文档） |
| C1 | 9/5 | **全链路演练**：主席登录 `/login` → `/work` → 点「招新管理」→ 确认能进（A3 生效验证）→ 导出 CSV → 手机填一份真报名 → 成功页二维码**用微信扫能进群** |
| C2 | 9/5 | **群码核对**：`team_qr` 11 项逐一扫码（A6 做完就在 `/work/site` 填，没做完用 `/cms`）；`consult_qr` 确认不是占位图 |
| C3 | 9/5 | **og 配图**（可选）：裁 `sc02.jpg` 为 1200×630、<300KB，上传 `static/uploads/og-recruit.jpg`，把两处 og:image 的 URL 尾巴换成它 |
| C4 | 9/6 早 7:30 | 主席手机开 4G（不开校园网）打开首页：3 秒内出海报层；报名提交一次成功 |
| C5 | 9/6 17:00 散场后立刻 | PA Bash：`cd ~/mysite && sqlite3 registrations.db "PRAGMA wal_checkpoint(TRUNCATE);" && tar czf ~/backup-20260906.tar.gz *.db && ls -la ~/backup-20260906.tar.gz`，然后 Files 标签下载到本地 + 微信群备份一份 |
| C6 | 9/6 17:10 | 导出 CSV 分发各分团长（名单当晚就要用） |

---

## 6. 批次 D · 招新后 backlog（下一轮会话按序做）

| # | 项 | 来源 | 要点 |
|---|---|---|---|
| D1 | pbkdf2 双读单写密码迁移 | P1-2 方案 A | werkzeug 自带，无新依赖；登录成功时静默升级；**先在本地 users.db 副本上全流程测一遍** |
| D2 | CSRF 防护 | P1-3 修正版 | 单点注入：`static/js/ui.js` 顶部加 fetch 包装器（12 个系统页**全部**已引 ui.js——已实测），token 走 `GET /api/csrf` 下发、header `X-CSRF-Token` 回传；豁免无会话接口（login/signup/reset）；后端 `before_request` 只拦 `session["uid"]` 存在的写请求。改动面因此从「40 接口 × 10 页面」缩到「1 个 JS + 1 个端点 + 1 个 before_request」 |
| D3 | 站点编辑二期（G7/G8） | 需求规格 §7 | `useSiteContent` Hook + `/api/site/published` 60s 缓存；只先做 Hero+Footer（需求规格 D4 建议 A）；`|| 默认值` 兜底；上线前本地 `npm run preview` 全屏走查 |
| D4 | cms 迁移收尾 | 需求规格 S3 + §13.3 | cms.html 全量迁 `/work/site` 后下线独立 cms；`/api/content` POST、`/api/kb` 写、`/api/upload` 切会话鉴权；`args.get("key")` 全库归零 |
| D5 | OSS 静态资源 | P1-5 + P0-2 方案 B | `CDN_BASE` 统一拼接函数先行，4 处 URL 后换 |
| D6 | 旧 `save_content()` 换原子写 | 需求规格 §13.2 | 一行改动：内部改调 `save_content_atomic(CONTENT, cur)` |
| D7 | 安全头收尾 | P1-4 | HSTS 试水（max-age=300 起）+ CSP Report-Only 跑一周 |
| D8 | schema 约束 / P0-8 gunicorn / P2 功能（通讯录→工作交接→资料库→看板→轮播 CMS 化）/ git 历史清理（P1-1B + 泄露 token 吊销 P2-6） | 各处 | 按问题清单原文执行 |

---

## 7. 风险矩阵（执行模型与 owner 各看一半）

| 风险 | 概率 | 爆点 | 缓解 | 责任 |
|---|---|---|---|---|
| content.json 写坏 → 招新页空白 | 低 | 招新中 | 原子写 + 30 份备份 + 回滚（A6）；上线前 owner 手动 copy 一份 content.json 到库外 | 执行模型 |
| A3 改鉴权漏接口 → 主席当天 403 | 中 | 9/6 17:00 导出 | C1 全链路演练兜底；403 时应急路径：PA Bash 临时加回兼容分支 | owner |
| WAL 开启后 tar 备份缺数据 | 中 | 9/6 备份 | C5 已含 wal_checkpoint | owner |
| build 后首页视觉回归 | 低 | 9/5 | B5 验收第 5 条逐屏目测；出问题 `git checkout` 产物回退上一版 landing/ | owner |
| 看门狗改坏 → 首页无背景 | 低 | 9/5 | B1 只动指定块；海报层天然兜底 | 执行模型 |
| SITE_EDIT_KEY 遗忘 | 低 | — | 存 config.json（gitignore 内）+ 记到 AI_CONTEXT | owner |
| 低端模型越权改禁区 | 中 | 任意 | §0.1 硬规则 + 每项 commit 单独可回滚 + owner review diff | owner |

---

## 8. 给下一个 AI 会话的入口

1. 先读本文档 §2 排期 + `施工进度.md`（执行模型维护的勾选表）确认推进到哪。
2. 批次 A/B 未完成 → 继续按工作项执行，协议见 §0。
3. 批次 A/B 已完成 → 从批次 D 的 D1 开始；D2 的「ui.js 单点注入」设计已在本档 §6 表内，直接实现。
4. 项目长期记忆见 `AI_CONTEXT.md`（§10 之后如有新章节以其为准）；运维命令见知识库「官网·运维速查」。

—— 总案完。祝招新顺利。
